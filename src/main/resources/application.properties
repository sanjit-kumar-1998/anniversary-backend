# Server Configuration
server.address=0.0.0.0
server.port=${PORT:8080}

# App Name
spring.application.name=anniversary-backend

# Unlock Date Configuration
app.unlock.date=2026-04-07T00:00:00
app.unlock.timezone=Asia/Kolkata

# Secure Password (use environment variable in production)
app.unlock.password=${APP_PASSWORD}